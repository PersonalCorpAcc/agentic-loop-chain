# Agent rules

This repository is small on purpose. Keep changes to `src/` and `test/`, keep the tests
passing with `pnpm test`, and do not edit anything under `.github/` -- those files are
projected from `.agentic/profile.yml` by the workflows package and any hand edit is
overwritten on the next install.
